<!-- pages/home-video/index.wxml -->
<wxs src="../../untils/format.wxs" module="format"></wxs>
<view class="video">
  <view wx:for="{{topMVs}}" wx:key="id" class="item">
    <video-item1 item="{{item}}" bind:tap="handleVideoClick" data-item="{{item}}"></video-item1>
  </view>
</view>